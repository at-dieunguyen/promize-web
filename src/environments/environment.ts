// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCSP8tsEEUMenmBHjdsWCJ6RnYwayrgQV4",
    authDomain: "promize-web.firebaseapp.com",
    databaseURL: "https://promize-web.firebaseio.com",
    projectId: "promize-web",
    storageBucket: "promize-web.appspot.com",
    messagingSenderId: "892901732352",
    appId: "1:892901732352:web:413f76c6750d30326568e5",
    measurementId: "G-7X0H6XDFMF"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
